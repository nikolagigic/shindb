import CollectionManager from "@/controllers/collection-manager.ts";
import Archive from "@/services/archive.ts";

if (import.meta.main) {
  const collectionManager = CollectionManager.getInstance();

  // const archive = Archive.getInstance();
}
